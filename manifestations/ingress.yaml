apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wil-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: wil-service  # Points to the Service
            port:
              number: 8888
